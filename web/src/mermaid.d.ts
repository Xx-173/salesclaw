/**
 * The pinned Mermaid package exposes its runtime module without a declaration
 * entry point under bundler resolution. Keep this narrow compatibility shim
 * until the dependency publishes compatible types.
 */
declare module 'mermaid';
