# SalesClaw 项目工作区说明

SalesClaw 是面向销售 SOP 与客户运营场景的自托管 Agent 工作台。本文件作为项目级运行上下文，适用于在此仓库中执行的 Agent。

## 工作原则

- 客户资料、会话内容、访问令牌、Cookie、模型密钥和渠道凭据均视为敏感数据；不得写入源码、日志、截图或提交记录。
- 默认只生成建议、草稿和可审计动作。涉及真实消息外发、价格、支付、折扣、客户承诺、退订或负面反馈时，需要经授权的流程和人工接管。
- 对消息渠道、工作区、工具和容器执行遵循最小权限原则；不要绕过 ACL、审计、频控或用户同意状态。
- 不要根据示例数据、旧记录或模型猜测虚构客户信息、业务指标或接口结果。

## 开发约定

- 修改 TypeScript 后至少运行对应的类型检查；涉及 Web 或 Electron 外观时检查品牌名称、图标和用户可见文案的一致性。
- 运行时配置通过环境变量和受控配置文件提供。密钥应由部署环境注入，不能提交到仓库。
- 项目级 `Skills`、MCP 与工作区文件属于显式配置边界，按 Workspace、用户和 Agent Profile 的权限策略加载。

## 销售场景边界

- 本仓库提供 Agent 运行时与治理能力，不代表已接入任何真实企微、CRM、支付、课程或客户外发渠道。
- 生产接入前必须完成渠道验签、客户同意校验、幂等、频控、停用开关、审计追踪和人工接管设计。
